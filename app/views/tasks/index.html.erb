<h1>タスク一覧画面</h1>
<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

  <% @tasks.each do |task| %>
  <tr>
    <td><%= task.title %></td>
    <td><%= task.content %></td>
  </tr>
  <% end %>
</table>

<%= link_to 'タスク作成', new_task_path %>